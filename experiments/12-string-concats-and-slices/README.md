# Experiment 12: String Concatenation

Demonstrates quadratic behavior of string concatenation in loops and the array+join mitigation.

**Key**: Use array.push() + join('') for building strings in loops.
