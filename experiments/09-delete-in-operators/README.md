# Experiment 09: Delete Operator

Demonstrates how `delete` causes shape changes and can push objects into dictionary mode.

**Key**: Use `null`/`undefined` instead of `delete` to preserve object shape.
