# Experiment: $(basename $exp)

## Purpose

TODO: Document what this experiment demonstrates.

## How to Run

```bash
npm run exp -- --exp $(basename $exp) --variant baseline
npm run exp -- --exp $(basename $exp) --variant deopt --trace on
npm run exp -- --exp $(basename $exp) --variant fixed
```

See `_template/README.md` for full structure.
