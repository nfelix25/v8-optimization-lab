# Experiment 07: Arguments vs Rest Parameters

Demonstrates performance difference between `arguments` object and rest parameters.

**Key**: Use rest parameters (`...args`) instead of `arguments` for better optimization.
